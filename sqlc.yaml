version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "queries/package.sql"
    schema: "queries/migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
