version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "queries/package.sql"
    schema: "queries/migrations"
    gen:
      go:
        package: "dbgen"
        out: "internal/db/dbgen"
        sql_package: "pgx/v5"
